export interface Card{
    card_image: string;
    name: string;
    colors: Array<string>;
    description: string;
    stats: string;
    types: string;
}

export interface User{
    username: string;
    password: string;
}

